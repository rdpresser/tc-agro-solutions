meta {
  name: GET ListProperties
  type: http
  seq: 1
}

get {
  url: {{farm_service_url}}/api/properties?ownerId={{owner_id}}&pageNumber=1&pageSize=20&sortBy=createdAt&sortDirection=desc&filter=
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

params:query {
  ownerId: {{owner_id}}
  pageNumber: 1
  pageSize: 20
  sortBy: createdAt
  sortDirection: desc
  filter: 
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

