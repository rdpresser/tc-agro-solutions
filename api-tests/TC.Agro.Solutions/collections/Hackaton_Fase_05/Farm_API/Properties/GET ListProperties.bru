meta {
  name: GET ListProperties
  type: http
  seq: 1
}

get {
  url: {{farm_service_url}}/api/properties?ownerId=&pageNumber=1&pageSize=20&sortBy=createdAt&sortDirection=desc&filter=
  body: none
  auth: bearer
}

params:query {
  ownerId: 
  pageNumber: 1
  pageSize: 20
  sortBy: createdAt
  sortDirection: desc
  filter: 
}

headers {
  accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
