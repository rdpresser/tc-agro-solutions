meta {
  name: GET ListPlotsFromProperty
  type: http
  seq: 5
}

get {
  url: {{farm_service_url}}/api/properties/{{id}}/plots?pageNumber=1&pageSize=20&sortBy=createdAt&sortDirection=desc&filter=&cropType=sample&cropTypeCatalogId={{crop_type_id}}
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

params:query {
  pageNumber: 1
  pageSize: 20
  sortBy: createdAt
  sortDirection: desc
  filter: 
  cropType: sample
  cropTypeCatalogId: {{crop_type_id}}
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

