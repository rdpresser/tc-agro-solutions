meta {
  name: GET GetPropertyById
  type: http
  seq: 3
}

get {
  url: {{farm_service_url}}/api/properties/{{id}}
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

