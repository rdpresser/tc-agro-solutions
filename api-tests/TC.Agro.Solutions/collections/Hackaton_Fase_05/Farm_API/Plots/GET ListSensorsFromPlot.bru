meta {
  name: GET ListSensorsFromPlot
  type: http
  seq: 4
}

get {
  url: {{farm_service_url}}/api/plots/{{id}}/sensors?pageNumber=1&pageSize=20&sortBy=createdAt&sortDirection=desc&filter=&type=Generic&status=Pending
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

params:query {
  pageNumber: 1
  pageSize: 20
  sortBy: createdAt
  sortDirection: desc
  filter: 
  type: Generic
  status: Pending
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

