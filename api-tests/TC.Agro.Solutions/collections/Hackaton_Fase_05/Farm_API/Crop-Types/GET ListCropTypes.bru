meta {
  name: GET ListCropTypes
  type: http
  seq: 1
}

get {
  url: {{farm_service_url}}/api/crop-types?ownerId={{owner_id}}&source=Manual&includeStale=false&includeInactive=false&pageNumber=1&pageSize=20&sortBy=createdAt&sortDirection=desc&filter=
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

params:query {
  ownerId: {{owner_id}}
  source: Manual
  includeStale: false
  includeInactive: false
  pageNumber: 1
  pageSize: 20
  sortBy: createdAt
  sortDirection: desc
  filter: 
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

