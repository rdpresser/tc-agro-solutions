meta {
  name: Re-Sync Users
  type: http
  seq: 5
}

post {
  url: {{identity_service_url}}/auth/resync/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
