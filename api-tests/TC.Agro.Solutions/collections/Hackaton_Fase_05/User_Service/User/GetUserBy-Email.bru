meta {
  name: GetUserBy-Email
  type: http
  seq: 1
}

get {
  url: {{identity_service_url}}/api/user/by-email/Admin@admin.com
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
