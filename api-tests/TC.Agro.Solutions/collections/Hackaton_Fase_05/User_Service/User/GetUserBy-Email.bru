meta {
  name: GetUserBy-Email
  type: http
  seq: 4
}

get {
  url: {{identity_service_url}}/api/user/by-email/admin1@admin.com
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
