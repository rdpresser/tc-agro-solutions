meta {
  name: GetUser - List
  type: http
  seq: 2
}

get {
  url: {{identity_service_url}}/api/user?pageNumber=1&pageSize=10&sortBy=id&sortDirection=desc&filter
  body: json
  auth: bearer
}

params:query {
  pageNumber: 1
  pageSize: 10
  sortBy: id
  sortDirection: desc
  filter: 
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
