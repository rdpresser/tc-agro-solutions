meta {
  name: GET GetUserByEmail
  type: http
  seq: 4
}

get {
  url: {{identity_service_url}}/api/user/by-email/{{user_email}}
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

