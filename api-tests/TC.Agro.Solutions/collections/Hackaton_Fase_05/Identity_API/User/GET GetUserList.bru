meta {
  name: GET GetUserList
  type: http
  seq: 1
}

get {
  url: {{identity_service_url}}/api/user?pageNumber=1&pageSize=20&sortBy=createdAt&sortDirection=desc&filter=
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

params:query {
  pageNumber: 1
  pageSize: 20
  sortBy: createdAt
  sortDirection: desc
  filter: 
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

