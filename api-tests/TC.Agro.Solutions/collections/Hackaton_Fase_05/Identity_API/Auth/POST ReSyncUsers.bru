meta {
  name: POST ReSyncUsers
  type: http
  seq: 7
}

post {
  url: {{identity_service_url}}/auth/resync/users
  body: none
  auth: bearer
}

headers {
  accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
