meta {
  name: List
  type: http
  seq: 5
}

get {
  url: {{farm_service_url}}/api/owners?pageNumber=1&pageSize=1000&sortBy=name&sortDirection=asc&filter
  body: none
  auth: bearer
}

params:query {
  pageNumber: 1
  pageSize: 1000
  sortBy: name
  sortDirection: asc
  filter: 
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
