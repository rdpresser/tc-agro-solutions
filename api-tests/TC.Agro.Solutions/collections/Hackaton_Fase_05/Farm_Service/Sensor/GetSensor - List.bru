meta {
  name: GetSensor - List
  type: http
  seq: 2
}

get {
  url: {{farm_service_url}}/api/sensor?pageNumber=1&pageSize=10&sortBy=installedAt&sortDirection=desc&filter
  body: none
  auth: bearer
}

params:query {
  pageNumber: 1
  pageSize: 10
  sortBy: installedAt
  sortDirection: desc
  filter: 
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
