meta {
  name: GetLatest
  type: http
  seq: 1
}

get {
  url: {{sensor_service_url}}/api/dashboard/latest?pageNumber=1&pageSize=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
