# =====================================================
# Loki Configuration - Local Development
# =====================================================
auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  log_level: info

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

# Limits and ingestion rules (OTLP support requires structured metadata)
limits_config:
  allow_structured_metadata: true
  volume_enabled: true
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 32

# ============================================================
# Structured Metadata (OTLP labels)
# ============================================================
# Loki 2.9+ supports receiving logs via OTLP HTTP endpoint.
# OTEL Collector converts log attributes → Loki structured metadata.
#
# For correlation_id (and other attributes) to appear in Explore label filters:
# 1. OTEL Collector's attributes_to_labels processor must convert to labels
# 2. Loki must accept them as structured metadata (allow_structured_metadata: true)
# 3. Attributes MUST be string, number, or boolean (primitive types)
#
# This config automatically handles attributes sent via OTLP endpoint.
# No additional label mappings needed if OTEL does conversion.
structured_metadata:
  # This section is optional in Loki 2.9+; OTLP native support is automatic
  # If needed for additional transformations, add mappings here
  # For now, rely on OTEL Collector's attributes_to_labels/logs processor
