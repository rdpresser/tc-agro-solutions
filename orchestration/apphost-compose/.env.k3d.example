# =====================================================
# TC Agro Solutions - k3d Cluster Configuration (SENSITIVE DATA ONLY)
# =====================================================
#
# 📝 INSTRUCTIONS:
# 1. Copy this file to `.env.k3d` (NOT TRACKED IN GIT)
# 2. Update values with your SENSITIVE data only
# 3. DO NOT commit `.env.k3d` to Git (already in .gitignore)
# 4. Non-sensitive data is stored in configmap.yaml (Kubernetes ConfigMap)
# 5. Use this for development in k3d cluster import workflow
#
# =====================================================
# SENSITIVE DATA ONLY
# =====================================================
# This file ONLY contains:
#   - Database passwords
#   - RabbitMQ credentials
#   - JWT secrets
#   - API tokens
#   - Authentication credentials
#
# Non-sensitive values (hosts, ports, URLs) go in configmap.yaml
# =====================================================

# =====================================================
# DATABASE - Password Only
# =====================================================
Database__Postgres__Password=postgres

# =====================================================
# MESSAGING - Credentials Only
# =====================================================
Messaging__RabbitMQ__UserName=guest
Messaging__RabbitMQ__Password=guest

# =====================================================
# AUTHENTICATION - JWT Secret Only
# =====================================================
Auth__Jwt__SecretKey=your-256-bit-secret-key-change-in-production-12345678901234567890

# =====================================================
# SERVICES PORTS (Reference - non-sensitive)
# =====================================================
Services__Identity__HttpPort=5001
Services__Farm__HttpPort=5002
Services__SensorIngest__HttpPort=5003
Services__AnalyticsWorker__HttpPort=5004
Services__Dashboard__HttpPort=5005

# =====================================================
# LOGGING (Reference - non-sensitive)
# =====================================================
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft_AspNetCore=Warning
Logging__LogLevel__System=Warning

# =====================================================
# TELEMETRY CONFIGURATION
# =====================================================
# ⚠️ NOTE: Telemetry (Grafana) configuration is in configmap.yaml
# No sensitive values in Telemetry section (only hosts, ports, protocols)
