apiVersion: v1
kind: Service
metadata:
  name: farm-service
  namespace: agro-apps
  labels:
    app: farm-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: farm-service
