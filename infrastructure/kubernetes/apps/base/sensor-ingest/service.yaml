apiVersion: v1
kind: Service
metadata:
  name: sensor-ingest-api
  namespace: agro-apps
  labels:
    app: sensor-ingest-api
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: sensor-ingest-api
