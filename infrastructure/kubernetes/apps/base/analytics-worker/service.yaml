apiVersion: v1
kind: Service
metadata:
  name: analytics-worker
  namespace: agro-apps
  labels:
    app: analytics-worker
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: analytics-worker
