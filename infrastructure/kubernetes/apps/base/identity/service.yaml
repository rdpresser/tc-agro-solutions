apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: agro-apps
  labels:
    app: identity-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: identity-service
