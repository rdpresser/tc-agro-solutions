apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: agro-apps

resources:
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
  - ingressroute.yaml

commonLabels:
  project: tc-agro-solutions
  managed-by: argocd
